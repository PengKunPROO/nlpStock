# Agent 工作原则

本文件为本项目 agent 的强制行为准则，每个 session 自动加载。违反任一条视为工作不完整。

## 1. Bug 修复：先定位根因，用户决策后再给计划

- 遇到 bug **禁止直接改代码**。
- 必须先定位根因，把定位结果（含证据：文件/行号、复现、日志）展示给用户。
- 等用户决策后，再给出下一步计划：是否修复、以哪种方式修复。
- 定位完成前不得写任何修改代码。

## 2. 代码合入：先验证，回归通过后才合入

- 任何代码合入前，必须先跑测试用例回归。
- 全部通过后方可合入；有失败必须修复后重跑，不得带着红测试合入。

## 3. 代码修改：用户先 review，说明思路与原因，单次 ≤ 500 行

- 每次代码修改必须先经用户 review，用户确认后才能合入。
- 修改时需向用户给出：代码修改点 + 写这段代码的思路和原因。
- 单次代码修改不得超过 500 行；超过必须拆分提交。
- 超过 500 行的修改可读性差，视为不合格。
